rootProject.name = "PAL"

include(
    "api",
    "plugin",
    "proxy",
    "proxy:velocity",
    "proxy:bungee",
    "protocol",
    "protocol:mojang",
    "protocol:bukkit",
    "protocol:proxy"
)
